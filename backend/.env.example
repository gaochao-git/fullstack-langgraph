# 服务配置
ENV=production
PORT=8000
HOST=0.0.0.0
WORKERS=2
LOG_LEVEL=INFO
LOG_DIR=./logs

# 模型配置
DEEPSEEK_API_KEY=your-key
DEEPSEEK_BASE_URL=https://api.deepseek.com

# ============= 数据库配置 =============
# LangGraph Checkpointer配置
CHECKPOINTER_TYPE=postgres  
CHECKPOINTER_HOST=82.156.146.51
CHECKPOINTER_PORT=5432
CHECKPOINTER_DB=langgraph_memory
CHECKPOINTER_USER=gaochao
CHECKPOINTER_PASSWORD=fffjjj

# Omind database
DATABASE_TYPE=mysql
DATABASE_HOST=82.156.146.51
DATABASE_PORT=3306
DATABASE_NAME=omind
DATABASE_USER=gaochao
DATABASE_PASSWORD=fffjjj

# ============= 认证配置 =============
# JWT认证配置
JWT_SECRET_KEY=Z6TuwhYF9UY0-EVTTAZ3z-RVC2tXSNiMCvLu52egJCY
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# 安全配置
SECRET_KEY=7ccRsANlPE-yqUgVSA8tEWtPHar60YtAUaQGg_UUsBo
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=30

# 密码策略配置
MIN_PASSWORD_LENGTH=8
REQUIRE_UPPERCASE=true
REQUIRE_LOWERCASE=true
REQUIRE_DIGITS=true
REQUIRE_SPECIAL_CHARS=true

# ============= 报警服务配置 =============
# 配置外部报警接口地址，系统会从该接口获取报警数据
# ALARM_API_URL=http://your-alarm-api-server/api/alarms
#
# 报警接口需要支持的查询参数：
# - alarm_level: 严重级别过滤，数组参数，如 alarm_level=P1&alarm_level=P2
# - alarm_time: 时间过滤，返回大于等于此时间的告警
# - team_tag: 团队标签过滤，数组参数，如 team_tag=数据库&team_tag=应用
# - idc_tag: 机房标签过滤，数组参数，如 idc_tag=北京&idc_tag=上海
# - alarm_ip: 主机IP过滤
# - page: 页码
# - page_size: 每页数量
#
# 报警接口需要返回的数据格式：
# {
#   "total": 100,
#   "page": 1,
#   "page_size": 50,
#   "data": [
#     {
#       "alarm_id": "事件ID",
#       "alarm_source": "数据源",
#       "alarm_key": "监控指标key",
#       "alarm_name": "报警名称",
#       "alarm_desc": "报警描述",
#       "alarm_level": "严重级别（如：P1, P2, 严重, 警告等）",
#       "alarm_time": "时间（ISO格式或时间戳）",
#       "alarm_ip": "主机IP",
#       "team_tag": "团队tag",
#       "idc_tag": "机房tag"
#     }
#   ]
# }

# ============= Zabbix服务配置（独立服务） =============
# Zabbix服务运行配置
ZABBIX_SERVICE_HOST=0.0.0.0
ZABBIX_SERVICE_PORT=8001

# Zabbix API连接配置
ZABBIX_API_URL=http://82.156.146.51:8080/zabbix/api_jsonrpc.php
ZABBIX_USERNAME=Admin
ZABBIX_PASSWORD=zabbix

# 使用Zabbix服务作为报警数据源
# ALARM_API_URL=http://localhost:8001/api/alarms
