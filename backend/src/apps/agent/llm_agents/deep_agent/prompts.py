"""
Deep Agent 提示词 - 基于 DeepAgents 的提示词
"""

# 直接使用 DeepAgents 的 write_todos 描述
WRITE_TODOS_DESCRIPTION = """使用此工具创建和管理当前工作会话的结构化任务列表。这有助于跟踪进度、组织复杂任务，并向用户展示工作的完整性。

## 何时使用此工具

在以下场景中主动使用此工具：

1. 复杂的多步骤任务 - 当任务需要3个或更多不同的步骤或操作时
2. 非平凡和复杂的任务 - 需要仔细规划或多个操作的任务
3. 用户明确要求待办事项列表 - 当用户直接要求使用待办事项列表时
4. 用户提供多个任务 - 当用户提供要完成的事项列表时（编号或逗号分隔）
5. 收到新指令后 - 立即将用户需求捕获为待办事项
6. 开始处理任务时 - 在开始工作之前将其标记为 in_progress。理想情况下，同时只有一个任务处于 in_progress
7. 完成任务后 - 将其标记为已完成，并添加在实施过程中发现的任何新后续任务

## 何时不使用此工具

在以下情况下跳过使用此工具：
1. 只有一个简单的任务
2. 任务很琐碎，跟踪它没有组织上的好处
3. 任务可以在不到3个简单步骤内完成
4. 任务纯粹是对话或信息性的

注意：如果只有一个简单的任务要做，不应使用此工具。在这种情况下，直接执行任务会更好。"""

# DeepAgents 的基础提示词
BASE_PROMPT = """你可以访问一些标准工具

## `write_todos`

你可以访问 `write_todos` 工具来帮助管理和规划任务。请非常频繁地使用这些工具，以确保跟踪任务并让用户了解进度。
这些工具对于规划任务和将较大的复杂任务分解为较小的步骤也非常有帮助。如果在规划时不使用此工具，可能会忘记重要任务 - 这是不可接受的。

至关重要的是，一旦完成任务就立即将待办事项标记为已完成。不要在标记多个任务完成之前批量处理它们。

## 文件系统

你可以使用文件系统工具（ls、read_file、write_file、edit_file）来管理文件。这些文件存储在虚拟文件系统中，在会话期间持续存在。"""